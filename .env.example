# Zobaczyć Morze - Konfiguracja środowiska
# ==========================================
# Skopiuj ten plik do .env i uzupełnij wartości:
#   cp .env.example .env
#
# UWAGA: Plik .env zawiera wrażliwe dane - NIGDY nie commituj go do repozytorium!

# ==============================================================================
# WYMAGANE
# ==============================================================================

# Klucz bezpieczeństwa Django (ZMIEŃ NA PRODUKCJI!)
# Wygeneruj nowy klucz komendą:
#   python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=zmien-mnie-na-bezpieczny-klucz
#klucz szyfrowania danych, wygeneruj komendą 
#python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
DJANGO_FIELD_ENCRYPTION_KEY=WKLEJ_TUTAJ_KLUCZ

# ==============================================================================
# ŚRODOWISKO
# ==============================================================================

# Tryb debugowania (True dla developmentu, False dla produkcji)
DEBUG=True

# Dozwolone hosty (oddzielone przecinkami)
# Dla developmentu zostaw puste, dla produkcji dodaj domenę
# Przykład: ALLOWED_HOSTS=zgloszenia.zobaczycmorze.pl,www.zobaczycmorze.pl
ALLOWED_HOSTS=

# URL strony (używany w linkach w emailach)
# Dla developmentu: http://localhost:8000
# Dla produkcji: https://zgloszenia.zobaczycmorze.pl
SITE_URL=http://localhost:8000

# ==============================================================================
# OPCJONALNE - Email
# ==============================================================================
# Domyślnie używany jest backend konsolowy (emaile wyświetlane w terminalu)
# Odkomentuj i uzupełnij dla prawdziwego wysyłania emaili

# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.example.com
# EMAIL_PORT=587
# EMAIL_HOST_USER=
# EMAIL_HOST_PASSWORD=
# EMAIL_USE_TLS=True
# DEFAULT_FROM_EMAIL=noreply@zobaczycmorze.pl

# ==============================================================================
# OPCJONALNE - Baza danych
# ==============================================================================
# Domyślnie używany jest SQLite (db.sqlite3)
# Odkomentuj dla PostgreSQL na produkcji

# DATABASE_URL=postgres://user:password@localhost:5432/zobaczycmorze

# ==============================================================================
# konfiguracja płatności payu
# ==============================================================================
#domyślnie jest santbox, wypełnij wszystkie pola na produkcji
PAYU_ENV=sandbox          # sandbox | production
PAYU_CLIENT_ID=123456
PAYU_CLIENT_SECRET=abcdef
PAYU_POS_ID=123456
WEBHOOK_SECRET=abcdef
PAYU_API_URL=example.com